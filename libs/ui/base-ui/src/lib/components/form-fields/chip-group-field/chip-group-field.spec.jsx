import React from 'react'
import {render} from '@testing-library/react'
import {BasicChipGroupField} from './chip-group-field.stories'

it('should render with the correct text', () => {
  // const { getByText } = render(<BasicChipGroupField />);
  // const rendered = getByText('hello from ChipGroupField');
  // expect(rendered).toBeTruthy();
})

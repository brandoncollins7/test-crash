import React from 'react'
import {render} from '@testing-library/react'
// import {BasicReactQuery} from './react-query.stories'

it('should render with the correct text', () => {
  // const {getByText} = render(<BasicReactQuery />)
  // const rendered = getByText('hello from ReactQuery')
  // expect(rendered).toBeTruthy()
})
